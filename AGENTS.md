# Agent Instructions

This repository's agent instructions live in [`CLAUDE.md`](CLAUDE.md). Read
that file first, then follow the "Read these first" reading order it links.

`AGENTS.md` exists so OpenAI Codex (and other tools that look for this
filename) pick up the same instructions Claude Code does. Do not edit this
file independently of `CLAUDE.md`; it is a pointer, not a second source of
truth.
